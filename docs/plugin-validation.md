# 插件版验证记录

对象：STATE Kit 插件 `0.3.7`，官方 Sub2API `v0.2.7`（`aea725f2ea644d5592d0bbb1d63b607efa7e200a`）。签名包包含 Linux amd64、Linux arm64 和 macOS arm64 运行时。

## 自动化验证

2026-09-21 在隔离构建环境中执行：

- `go test -race ./...`：通过。
- `go vet ./...`：通过。
- `node --test ui-tests/*.test.cjs`：24 项通过。
- `node --check ui/assets/app.js` 与 `node --check ui/assets/bridge-v1.js`：通过。
- `.s2plugin` 清单哈希、Ed25519 发布者签名和包内路径：通过。
- 使用官方 Sub2API `v0.2.7` 安装器和真实插件进程的宿主契约测试：通过。
- Playwright 桌面视口与 `390 × 844` 移动视口：实时诊断监听、面板打开/关闭、出口一致/不一致和 292/312 渲染通过，无页面级横向溢出。

测试覆盖默认关闭、账号隔离、Pro / Team 筛选、Sub2 原有代理与插件固定出口两种账号模式、代理链、采集与固定出口复验、持久化恢复、失败续期保留旧票据、异常守护、流式转发、取消与并发。

实时诊断测试覆盖未打开面板时不采集、连续状态轮询建立监听、关闭后过期清空、240 条环形上限、HTTP 状态与结果分类、出口 IP 一致性判断，以及票据、Token、代理凭据和请求头脱敏。

网页测试覆盖沙箱消息来源和 Token 校验、超时、加载与保存、账号资料校验、账号级出口模式和粘性代理校验、账号下拉显示 ID/名称/邮箱、模型建议与自定义输入、只读状态刷新、未保存修改保留、打开面板才监听、新事件置顶、关闭清空及敏感字段遮挡。

## 宿主和生产验收

- 使用官方 Sub2API `v0.2.7` 安装包协议完成签名安装和插件进程启动。
- 在现有测试实例上将 `v0.3.6` 停用后升级到 `v0.3.7`，再重新启用；插件 ID 保持不变。
- 配置页通过宿主只读 Bridge 成功读取账号管理和 IP 管理代理列表。
- 账号资料区显示并保存账号名称、邮箱、到期时间和额度摘要。
- 已发现账号下拉框显示 `账号 ID + 名称 + 邮箱`。
- 运行状态显示 `账号：ID + 名称` 和 `模型：模型名`。

验收页面未保存截图；真实账号资料只存在于运行实例的加密插件配置中，不进入源码、签名包、源码包或 Release。

## 当前签名包

```text
sub2api-state-kit_plugin_v0.3.7.s2plugin
最终文件大小与 SHA256 以 Release 中 `SHA256SUMS` 为准。

源码包的最终 SHA256 以 Release 中 `SHA256SUMS` 为准。
```

## 未包含的验证

- 没有使用真实 OAuth 账号对每个模型执行生产压测。
- 不承诺上游一定返回符合长度条件的 STATE，也不保证消除配额、429 或上游过载。
- 实时诊断只证明监听面板打开期间采集、业务请求和实际出口是否一致，以及上游当次返回的 STATE 长度分类和响应模型；它不会主动修复上游路由，也不能单独证明出口不一致是产生 312 的唯一原因。
- 自定义宿主镜像需要包含只读 `accounts.list` Bridge；官方原版宿主只提供账号 ID。插件在 Bridge 不可用时会退化为只显示账号 ID。
- 不支持多副本协调；当前部署范围为单个 Sub2API 应用实例。
